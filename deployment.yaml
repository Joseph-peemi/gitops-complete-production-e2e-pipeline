apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      gitops-complete-production-e2e-pipeline:apps/Deployment:default/terraform-pipeline-production
  labels:
    app: terraform-pipeline-production
  name: terraform-pipeline-production-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: terraform-pipeline-production
  template:
    metadata:
      labels:
        app: terraform-pipeline-production
    spec:
      containers:
        - name: terraform-pipeline-production
          image: abuchijoe/register-app-pipeline:latest

          ports:
            - containerPort: 8080

          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 20
            failureThreshold: 3

          env:
            - name: JAVA_TOOL_OPTIONS
              value: >
                -Xms256m
                -Xmx768m
                -XX:+UseContainerSupport
                -XX:+ExitOnOutOfMemoryError

          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1
              memory: 1024Mi
