apiVersion: v1
kind: Service
metadata:
  name: Terraform-Pipeline-Production-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: Terraform-Pipeline-Production
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 31918
      protocol: TCP